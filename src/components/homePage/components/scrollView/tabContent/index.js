import React from "react";
import { View, Dimensions } from "react-native";

const WIDTH = Dimensions.get("window").width;

export default function TabContent({ tab, templates }) {
	return (
		<View
			style={{
				position: "relative",
				width: WIDTH - 40,
			}}></View>
	);
	e;
}
